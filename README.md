# Neon portfolio

A small Angular landing page for Neon, an independent creative studio.

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:4200` in your browser.

## Build

```bash
npm run build
```
